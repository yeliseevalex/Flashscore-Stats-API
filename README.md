Проєкт на FastAPI для збору, зберігання та аналізу статистики футбольних матчів з сайту Flashscore.
Використовується Scrapy для парсингу матчів та подій, дані зберігаються в PostgreSQL.

> Частина коду парсера та внутрішніх алгоритмів прихована з міркувань конфіденційності.

---

## 🔹 Функціонал

* Асинхронний збір даних матчів та подій (Scrapy + SQLAlchemy)
* REST API для:

  * Останніх матчів команди
  * Особистих зустрічей команд
  * Прогнозів рахунків та ймовірності перемоги
  * Статистики голів та карток по хвилинах
  * Динаміки результативності команди
  * Фільтру матчів за лігами та сезонами

---

## 🔹 Технології

* Python
* FastAPI
* SQLAlchemy
* PostgreSQL
* Scrapy
* Docker

---

## 🔹 Структура проєкту

```
flashscore/
│
├─ src/
│  ├─ api/                 # Всі REST API
│  ├─ core/                # Конфігурація, DB
│  ├─ models/              # Моделі SQLAlchemy
│  ├─ repository/          # Репозиторії роботи з БД
│  ├─ schema/              # Pydantic схеми
│  ├─ services/            # Логіка розрахунку статистики та прогнозів
│  ├─ scrapy_app/           # Scrapy-парсер
│  └─ main.py              # Точка входу FastAPI
│
├─ docker/                 
│  └─ entrypoint.sh        # Скрипт запуску FastAPI або Scrapy
├─ Dockerfile              # Єдиний Dockerfile для FastAPI + Scrapy
├─ requirements.txt
└─ README.md
```

---

## 🔹 Приклади ендпоінтів FastAPI

* Прогноз рахунку: `/prediction/advanced/{home_team_id}/{away_team_id}`
* Ймовірність перемоги: `/prediction/win_probability/{home_team_id}/{away_team_id}`
* Останні матчі команди: `/teams/{team_id}/last_matches`
* Середні голи команди: `/teams/{team_id}/avg_goals`
* Динаміка результативності: `/teams/{team_id}/performance_trend`
* Статистика голів по хвилинах: `/analytics/goals_by_minute/{team_id}`
* Статистика карток по хвилинах: `/analytics/cards_by_minute/{team_id}`
* Фільтр матчів: `/matches/filter?league_id={id}&season={season}`
* Особисті зустрічі: `/matches/head_to_head/{team1_id}/{team2_id}`

---

## 🔹 Примітки

* Після старту контейнера база PostgreSQL створюється автоматично.
* Скрипти міграції та створення таблиць виконуються при `on_startup` FastAPI.
* Частина коду, що стосується парсера та внутрішньої логіки розрахунку прогнозів, прихована з міркувань безпеки.
